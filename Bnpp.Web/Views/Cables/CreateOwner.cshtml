@model Bnpp.DataLayer.Entities.Owner
@{
    ViewData["Title"] = "CreateOwner";
    Layout = null;
}

<h2>Create Owner</h2>

<div id="related">
    <div id="normal">
        <form id="designForm" asp-action="CreateOwner" method="post">
            <table cellpadding="3" cellspacing="0">
                <tbody>
                    <tr>
                        <td>
                            Owner Title:
                        </td>
                        <td>
                            <input asp-for="OwnerTitle" type="text" style="direction: ltr; text-align: left;">
                        </td>
                        <td>
                            <span asp-validation-for="OwnerTitle"></span>
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <input type="button" onclick="AjaxdocumentFormSubmit()" value="Save" id="btnGet" class="butn-save" style="direction: ltr; text-align: left;">
                            <input type="button" onclick="ShowOwner()" value="Cancel" id="Button2" class="butn-cancel" style="direction: ltr; text-align: left;">

                        </td>
                    </tr>
                </tbody>
            </table>
        </form>
    </div>
</div>

<script src="/js/jquery.min.js"></script>
<script src="/js/kendo.all.min.js"></script>
<script src="~/js/jquery.validate.min.js"></script>
<script src="~/js/jquery.validate.unobtrusive.min.js"></script>


<script type="text/javascript">

    function ShowOwner() {


        if (@ViewBag.Code == 1) {
            $("#newData").load("/Cables/EditOwner/");
        }
        //$("#newData").load("/Cables/Owner/");
    }

    function AjaxdocumentFormSubmit() {
        var data = $("#designForm").serialize();
       
        $.ajax({
            type: 'POST',
            url: '/Cables/CreateOwner/',
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8', // when we use .serialize() this generates the data in query string format. this needs the default contentType (default content type is: contentType: 'application/x-www-form-urlencoded; charset=UTF-8') so it is optional, you can remove it
            data: data,
            success: function () {
                alert(@ViewBag.Code);
                $("#newData").load("/Cables/Owner/");
            },

            error: function () {
                //alert('Failed to receive the Data');
                //console.log('Failed ');
            }
        });

    }

</script>



